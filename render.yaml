services:
  - type: web
    name: cat-game-arcade
    runtime: node
    plan: free
    branch: main
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /api/big2/health
